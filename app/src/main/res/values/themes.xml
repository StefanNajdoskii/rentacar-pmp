<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.RentaCar" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryContainer">@color/primary_container</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryContainer">@color/secondary_container</item>

        <!-- Background: set every flavor (system attr, Material attr, window bg) so views that
             read any of them — and the window itself — switch with the night-mode resource. -->
        <item name="android:colorBackground">@color/background</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="android:windowBackground">@color/background</item>

        <!-- Surface: cards, sheets, dialogs all draw from this. -->
        <item name="colorSurface">@color/surface</item>
        <item name="colorSurfaceVariant">@color/surface</item>

        <!-- "On" colors: text/icon contrast. Without these, Material defaults stay fixed
             and text appears washed out against the swapped backgrounds. -->
        <item name="colorOnBackground">@color/on_background</item>
        <item name="colorOnSurface">@color/on_background</item>
        <item name="colorOnSurfaceVariant">@color/on_background</item>

        <item name="colorError">@color/error</item>
    </style>

    <style name="Theme.RentaCar.NoActionBar" parent="Theme.RentaCar" />

    <style name="Theme.SplashScreen" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:navigationBarColor">@color/background</item>
    </style>
</resources>
